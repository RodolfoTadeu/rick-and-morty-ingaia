export const border = () => {
    return `
      height: 4.2rem;
      border: 2px solid var(--white);
      border-radius: 8px;
      background: transparent;
      color: var(--white);
      font-family: Nunito;
      font-size: 1.6rem;
      font-weight: 700;
    `;
};
